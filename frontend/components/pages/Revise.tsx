'use client';

// Ported in a follow-up migration sub-commit (wires to /api/revise).
export default function Revise() {
	return <div className="p-4 text-sm text-zinc-500">Revise panel coming soon.</div>;
}
