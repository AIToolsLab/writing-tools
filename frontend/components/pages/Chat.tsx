'use client';

// Ported in a follow-up migration sub-commit (wires to /api/chat via useChat).
export default function Chat() {
	return <div className="p-4 text-sm text-zinc-500">Chat panel coming soon.</div>;
}
